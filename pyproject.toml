[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "aml-mule-detection"
version = "0.1.0"
description = "AML money-mule detection pipeline"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]